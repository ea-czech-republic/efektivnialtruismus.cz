pipeline:
  build-dev:
    image: plugins/docker
    settings:
      repo: czea/et
      registry: index.docker.io
      tags: latest
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
  linter:
    image: python:alpine
    commands:
      - echo "hey"